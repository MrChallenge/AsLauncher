<UserControl x:Class="AsLauncher.Views.Pages.VanillaPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:lang="clr-namespace:AsLauncher.Resources.Localization"
             xmlns:core="clr-namespace:AsLauncher.Core"
             xmlns:components="clr-namespace:AsLauncher.Views.Components"
             >

    <Grid Background="{x:Static core:Theme.Background}">

        <Grid Margin="24">

            <Grid.RowDefinitions>

                <RowDefinition Height="Auto"/>             
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>

            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                       Text="{x:Static lang:Resources.MinecraftVersionsList}"
                       FontSize="24"
                       FontWeight="SemiBold"
                       Foreground="{x:Static core:Theme.White}"
                       Margin="0,0,0,24"/>

            <Grid Grid.Row="1" Margin="0,0,0,12">

                <Grid.ColumnDefinitions>

                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="220"/>

                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0"
                            Orientation="Horizontal"
                            VerticalAlignment="Center">

                    <CheckBox x:Name="ReleaseCheckBox"
                              Style="{StaticResource ModernCheckBox}"
                              Content="{x:Static lang:Resources.MinecraftVersionRelease}"
                              Checked="FilterChanged"
                              Unchecked="FilterChanged"
                              Foreground="{x:Static core:Theme.White}"
                              IsChecked="True"/>

                    <CheckBox x:Name="SnapshotCheckBox"
                              Style="{StaticResource ModernCheckBox}"
                              Content="{x:Static lang:Resources.MinecraftVersionSnapshot}"
                              Margin="16,0,0,0"
                              Checked="FilterChanged"
                              Unchecked="FilterChanged"
                              Foreground="{x:Static core:Theme.White}"
                              IsChecked="True"/>

                    <CheckBox x:Name="BetaCheckBox"
                              Style="{StaticResource ModernCheckBox}"
                              Content="{x:Static lang:Resources.MinecraftVersionBeta}"
                              Margin="16,0,0,0"
                              Checked="FilterChanged"
                              Unchecked="FilterChanged"
                              Foreground="{x:Static core:Theme.White}"
                              IsChecked="True"/>

                    <CheckBox x:Name="AlphaCheckBox"
                              Style="{StaticResource ModernCheckBox}"
                              Content="{x:Static lang:Resources.MinecraftVersionAlpha}"
                              Margin="16,0,0,0"
                              Checked="FilterChanged"
                              Unchecked="FilterChanged"
                              Foreground="{x:Static core:Theme.White}"
                              IsChecked="True"/>

                </StackPanel>

                <Border Grid.Column="2"
                        Height="24"
                        Background="{x:Static core:Theme.Middleground}"
                        BorderBrush="{x:Static core:Theme.Foreground}"
                        BorderThickness="1"
                        CornerRadius="6"
                        VerticalAlignment="Center">

                    <Grid>

                        <TextBlock Text="{x:Static lang:Resources.MinecraftVersionSearchExample}"
                                   Margin="8,0,0,0"
                                   VerticalAlignment="Center"
                                   Foreground="{x:Static core:Theme.LightGrey}"
                                   IsHitTestVisible="False">

                            <TextBlock.Style>

                                <Style TargetType="TextBlock">

                                    <Setter Property="Visibility" Value="Collapsed"/>

                                    <Style.Triggers>

                                        <DataTrigger Binding="{Binding Text, ElementName=SearchBox}" Value="">

                                            <Setter Property="Visibility" Value="Visible"/>

                                        </DataTrigger>

                                    </Style.Triggers>

                                </Style>

                            </TextBlock.Style>

                        </TextBlock>

                        <TextBox x:Name="SearchBox"
                                 Background="{x:Static core:Theme.Transparent}"
                                 Foreground="{x:Static core:Theme.White}"
                                 CaretBrush="{x:Static core:Theme.White}"
                                 SelectionBrush="{x:Static core:Theme.Blue}"
                                 SelectionOpacity="0.35"
                                 BorderThickness="0"
                                 Padding="8,0"
                                 VerticalContentAlignment="Center"
                                 TextChanged="SearchBox_TextChanged"/>

                    </Grid>

                </Border>

            </Grid>

            <ListBox x:Name="VersionList"
                     Grid.Row="2"
                     ItemsSource="{Binding Versions}"
                     Background="{x:Static core:Theme.Transparent}"
                     BorderThickness="0"
                     ScrollViewer.VerticalScrollBarVisibility="Hidden"
                     VirtualizingPanel.IsVirtualizing="True"
                     ScrollViewer.CanContentScroll="True">

                <ListBox.ItemTemplate>

                    <DataTemplate>

                        <components:MinecraftVersionCard Version="{Binding}" />

                    </DataTemplate>

                </ListBox.ItemTemplate>

                <ListBox.ItemContainerStyle>

                    <Style TargetType="ListBoxItem">

                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Margin" Value="0"/>
                        <Setter Property="Focusable" Value="False"/>
                        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                        <Setter Property="IsTabStop" Value="False"/>

                        <Setter Property="Template">

                            <Setter.Value>

                                <ControlTemplate TargetType="ListBoxItem">

                                    <ContentPresenter/>

                                </ControlTemplate>

                            </Setter.Value>

                        </Setter>

                    </Style>

                </ListBox.ItemContainerStyle>

            </ListBox>

        </Grid>

    </Grid>

</UserControl>